# HANDOFF

**Незавершённой работы нет.** Перенос библиотек `wtpkcs11ecp` завершён.

Дерево версии `2.18.2.0` опубликовано и проверено по HTTPS; загрузчик, тесты и три workflow
переведены на прямые URL с закреплёнными SHA-256. Локально и в CI проходят 92 теста; сборки
Linux, macOS и Windows загрузили новые библиотеки, запустили приложение и сохранили пакеты.
Четыре прежних бинарных ZIP удалены из релиза `3rdparty`; проверенная резервная копия лежит
в `backups/wallet-tool/3rdparty-binary-assets-20260821`.

Открытые пункты, никем не взятые, — в конце `docs/PLAN.md`: подтверждение для `--force`,
подпись ключами `ed25519` и BIP39-passphrase. Они отложены владельцем сознательно,
начинать их без его слова не нужно.

**Отдельно:** ничего из операций с токеном не проверялось на реальном кошельке —
устройства нет. В первую очередь это касается смены кривой: ключи, созданные прежними
версиями утилиты, остались на `secp256r1`, и одна и та же мнемоническая фраза до и после
правки даёт разные ключи. Мок-тесты этого не покажут.
