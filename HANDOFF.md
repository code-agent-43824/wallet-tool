# HANDOFF

**В работе:** внедрение правил разработки (`AGENTS.md`), карты кода (`CLAUDE.md`) и
рабочих документов. Ветка не используется — коммиты идут прямо в `main` (§1), с явного
разрешения владельца.

**Следующий шаг:** написать `CLAUDE.md` — импорт `AGENTS.md` плюс карта кода, затем добавить
`.github/workflows/tests.yml` и `requirements-dev.txt`, чтобы `pytest` гонялся в CI.

**Код не тронут.** Изменения пока только в документах.
