# HANDOFF

**В работе:** внедрение правил разработки. `AGENTS.md`, `CLAUDE.md` и рабочие документы
готовы. Ветка не используется — коммиты идут прямо в `main` (§1), с явного разрешения
владельца.

**Следующий шаг:** добавить `.github/workflows/tests.yml` и `requirements-dev.txt`, чтобы
`pytest` гонялся в CI. Сейчас 47 тестов проходят только локально.

**Код не тронут.** Изменения пока только в документах.
