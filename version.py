"""Единственный источник правды о версии.

Формат совпадает с тегами релизов: версия ``0.7`` выпускается тегом ``v0.7``.
Здесь лежит версия, с которой выйдет **следующий** релиз, — поднимать её нужно
тем же изменением, которым проект готовят к выпуску, а не после него.
"""

__version__ = "0.7"
