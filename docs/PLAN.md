# PLAN — текущий этап

**Этап: внедрение правил разработки и карты кода.**

Что делаем сейчас, чтобы следующий агент входил в репозиторий по документам, а не по диффу.

- [x] Положить канонический `AGENTS.md` в корень и дополнить приложением по проекту:
      команды, settled decisions, отступления, версии, деплой, что проверяет владелец.
- [x] Написать `CLAUDE.md`: импорт `AGENTS.md` плюс карта кода — архитектура, точки входа,
      неочевидные связки, подводные камни, чего в репозитории нет.
- [x] Завести рабочие документы: `docs/STATUS.md`, `docs/WORKLOG.md`, `HANDOFF.md`.
- [x] Запустить тесты в CI. 47 тестов лежали в `tests/`, но ни один workflow их не
      вызывал — правило «trunk всегда зелёный» ничем не подкреплялось. Добавлены
      `.github/workflows/tests.yml` и `requirements-dev.txt` (pytest нигде не был объявлен).

Дальше — то, что нашлось по ходу и требует решения владельца. Не начато.

- [ ] Явный `_layout_` у ctypes-структур с `_pack_`. В логе первого прогона CI (Python
      3.14.7) три предупреждения: «Due to `_pack_`, the ... Structure will use memory layout
      compatible with MSVC (Windows). If this is intended, set `_layout_` to `'ms'`. The
      implicit default is deprecated and slated to become an error in Python 3.19.»
      Задевает `CK_ATTRIBUTE`, `CK_MECHANISM` и
      `CK_VENDOR_BIP32_WITH_BIP39_KEY_PAIR_GEN_PARAMS`. Сейчас это только предупреждение,
      но на Python 3.19 сборка перестанет импортироваться. Выбор `_layout_` — решение о
      раскладке памяти в вызовах к вендорной библиотеке: ошибка не упадёт, а тихо сдвинет
      поля, и mock-тесты этого не поймают. Менять только с проверкой на реальном кошельке.
- [ ] Пин вендорной библиотеки по SHA-256. §7 требует тянуть вендорные бинарники
      «pinned by SHA-256», а `scripts/download_wtpkcs11ecp.py` опирается на тег релиза
      `3rdparty` и id ассета — контрольная сумма не проверяется. Ассет в релизе можно
      заменить, и сборка этого не заметит.
- [ ] Вопрос владельцу: PIN-код передаётся аргументом `--pin`, то есть попадает в историю
      оболочки и в список процессов. Оставляем как есть (тогда это settled decision с
      причиной) или добавляем чтение из stdin / переменной окружения?
- [ ] Вопрос владельцу: нужна ли версия. Сейчас в репозитории нет ни `__version__`, ни
      тегов, ни changelog — релизы собираются вручную и ничем не помечены.
